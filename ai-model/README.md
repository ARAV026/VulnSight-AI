# AI Model Module

This module contains a training scaffold for severity classification and a lightweight runtime predictor.

Files:

- `train_model.py`: trains and serializes a demo severity classifier
- `predictor.py`: loads the model for runtime use
